


@Retention ：它所注解的注解保留多久
SOURCE
被编译器忽略,源码注解

CLASS
注解将会被保留在Class文件中，但在运行时并不会被VM保留。这是默认行为，所有没有用Retention注解的注解，都会采用这种策略。

RUNTIME
保留至运行时。所以我们可以通过反射去获取注解信息。


@Target：注解作用范围：FIELD 属性  METHOD方法  TYPE 放在类上



